import React from 'react'
import { ScrollLinkId } from '../utils/scrollLinkId.enum'
import Container from './container'

const Landing = () => {
  return <Container scrollLinkId={ScrollLinkId.Landing}>Landing</Container>
}

export default Landing
