import React from 'react'
import { ScrollLinkId } from '../constants/scrollLinkId.enum'
import Container from './container'

const Work = () => {
  return <Container scrollLinkId={ScrollLinkId.Work}>Work</Container>
}

export default Work
