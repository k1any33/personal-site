import React from 'react'
import { ScrollLinkId } from '../constants/scrollLinkId.enum'
import Container from './container'

const AboutMe = () => {
  return <Container scrollLinkId={ScrollLinkId.AboutMe}>About Me</Container>
}

export default AboutMe
